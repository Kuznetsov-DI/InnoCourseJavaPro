version: '3'

services:
  postgresql:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: java_pro_course
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 123456
    ports:
      - 5432:5432