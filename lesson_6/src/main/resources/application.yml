server:
  port: 8989

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/java_pro_course
    username: root
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none

  flyway:
    baseline-on-migrate: true
